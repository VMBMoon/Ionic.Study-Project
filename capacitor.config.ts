import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Atividade_Ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
